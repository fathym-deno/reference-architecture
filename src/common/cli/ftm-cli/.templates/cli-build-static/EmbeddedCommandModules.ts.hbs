{{! embedded-commands.ts.hbs }}

{{#each imports}}
import {{alias}} from "{{path}}";
{{/each}}

export const EmbeddedCommandModules = {
{{#each modules}}
  "{{key}}": {{alias}},
{{/each}}
};
